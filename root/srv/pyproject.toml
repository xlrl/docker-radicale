[project]
name = "radicale-server"
version = "3.7.7"
description = "This is the radicale server instance"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bcrypt",
    "passlib",
    "pytz",
    "radicale==3.7.7",
]
