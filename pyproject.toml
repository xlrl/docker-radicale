[project]
name = "docker-radicale"
version = "3.7.5"
description = "docker-radicale (Python support scripts for this repository)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests",
]
